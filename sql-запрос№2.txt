SELECT 
    Родитель.Наименование AS "Категория",
    COUNT(Дочерняя.ID) AS "Количество дочерних элементов"
FROM КаталогНоменклатуры Родитель
LEFT JOIN КаталогНоменклатуры Дочерняя ON Родитель.ID = Дочерняя.IDРодителя
GROUP BY Родитель.Наименование